#!/bin/sh
# Strip Cursor co-author trailers added by the IDE agent.
sed -i.bak '/^Co-authored-by: Cursor <cursoragent@cursor.com>/d' "$1" 2>/dev/null || \
  sed -i '' '/^Co-authored-by: Cursor <cursoragent@cursor.com>/d' "$1" 2>/dev/null
rm -f "$1.bak"
